/**
 * ====================================
 * TASK PRIORITY ENUM
 * ====================================
 * Defines priority levels for tasks
 */

export enum TaskPriority {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
  URGENT = 'urgent',
}

export default TaskPriority;