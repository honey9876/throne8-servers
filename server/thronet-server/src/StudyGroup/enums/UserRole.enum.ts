/**
 * ====================================
 * USER ROLE ENUM
 * ====================================
 * Defines different user roles in the system
 */

export enum UserRole {
  STUDENT = 'student',
  MENTOR = 'mentor',
  ADMIN = 'admin',
  USER = 'user'
}

export default UserRole;