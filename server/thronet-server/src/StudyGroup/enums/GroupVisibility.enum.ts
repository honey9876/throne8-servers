/**
 * ====================================
 * GROUP VISIBILITY ENUM
 * ====================================
 * Defines visibility settings for groups
 */

export enum GroupVisibility {
  PUBLIC = 'public',
  PRIVATE = 'private',
}

export default GroupVisibility;